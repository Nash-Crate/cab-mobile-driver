import 'package:flutter/material.dart';

/// Trip start widget
class TripStart extends StatelessWidget {
  // ignore: public_member_api_docs
  const TripStart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('trip start');
  }
}
